# Handoff — Priority 1–4 hardening: mid-execution, Task 2 blocked on earning idempotency

**Working dir:** /Users/sam/code/journal-app (execution happens in worktree /Users/sam/code/journal-app-hardening, branch `fix/priority-hardening`)
**Project:** Daily journaling app (Next.js 16 + Prisma 6/SQLite + NextAuth v5) with streaks, rules, achievements, freeze/shield inventory; prod on EC2/PM2.

## What we did
- Ran a full multi-agent review of code + docs → wrote `docs/newissues.md` (33 new issues, deduped against ISSUES.md/audit docs), with priority order.
- Wrote 12-task implementation plan for priorities 1–4 → `docs/superpowers/plans/2026-06-09-priority-hardening.md`.
- Backed up local dev DB → `~/backups/journal-app/dev-20260609-144628.db` (prod backup deferred to deploy time; plan touches no prod data).
- Created worktree `../journal-app-hardening` on branch `fix/priority-hardening`; executing subagent-driven (implementer → spec review → quality review per task).
- **Task 1 complete** (vitest infra + throwaway SQLite testDb helper): commits `842adfd`, `b1c41bf`. Both reviews passed.
- **Task 2 implemented** (`lib/inventoryEarning.ts`, transactional earning): commit `aeb1e01`, spec review ✅, quality review approved logic but required 2 extra tests → **one of them found a real design flaw (BLOCKED, see below)**.
- Committed plan + newissues into the branch: `1b44de3`.

## State of play
- **BLOCKER (Task 2):** `processFirstEntryEarning` is not idempotent under concurrent/double invocation. The new concurrency test fails deterministically (5/5): two racing calls BOTH return true and the counter reaches 2. Mechanism: SQLite serializes the transactions fine, but the guard (`todayEntryCount > entriesJustCreated`) is never mutated by the transaction itself, so the second serialized transaction passes the guard again and double-increments. Transactions fixed lost-updates but not double-earn.
- **Uncommitted in worktree:** JSDoc fix + `entriesJustCreated <= 0` guard in `lib/inventoryEarning.ts`, plus 2 new tests in `tests/lib/inventoryEarning.test.ts` (1 failing: "never double-counts under concurrent invocation"). Deliberately not committed (red suite).
- Likely fix (no schema change, stays in plan scope): store `lastEarnedDay: 'YYYY-MM-DD'` inside the existing `metadata` JSON and have the transaction guard on + write it — the transaction then mutates its own predicate. Day string must be passed in by caller (it already computes user-timezone day). Alternative: a marker row with unique constraint = schema change (plan explicitly excludes schema changes).
- Tasks 3–12 not started. Harness task list has all 12 (Task 1 ✅, Task 2 in_progress).
- Sam's conditions: feature branch ✓, DB backups before risky steps ✓, never touch prod DB, subagent-driven always, local test then user approval before merge/deploy.

## Key files
- `docs/newissues.md` — the review findings (N-numbered issues this work fixes)
- `docs/superpowers/plans/2026-06-09-priority-hardening.md` — the 12-task plan (full code in every step)
- Worktree: `lib/inventoryEarning.ts` + `tests/lib/inventoryEarning.test.ts` — blocked Task 2 artifacts (uncommitted changes present)
- Worktree: `tests/helpers/testDb.ts`, `tests/helpers/fixtures.ts` — Task 1 test infra used by all DB tests
- `~/backups/journal-app/dev-20260609-144628.db` — dev DB backup

## Next session — options

### Option 1: Fix the Task 2 blocker, then resume the plan (recommended)
The double-earn flaw is real and the fix is small; everything downstream is already planned.

```
Continue executing docs/superpowers/plans/2026-06-09-priority-hardening.md (subagent-driven, per the plan header) in the worktree /Users/sam/code/journal-app-hardening on branch fix/priority-hardening. First resolve the Task 2 blocker described in docs/nextPrompt20260609-150941.md: make processFirstEntryEarning idempotent by storing lastEarnedDay (user-timezone YYYY-MM-DD, passed in by the caller as a new param) in the UserInventory metadata JSON, guarding on it inside the transaction. Update the failing concurrency test to assert counter <= 1 and add a same-args-double-call idempotency test. Note tests/lib/inventoryEarning.test.ts and lib/inventoryEarning.ts have uncommitted changes in the worktree — review them first. Then re-run quality review for Task 2 and continue Tasks 3–12. Task 3's wiring must pass the day string it already computes. No schema changes, never touch dev.db or prod.
```

### Option 2: Park the concurrency fix, continue Tasks 3–12 first
If you'd rather bank the other seven fixes (org scoping, autosave UX, docs) and decide on the earning design with fresh eyes — the blocker only affects double-earn, and the committed code is still strictly better than main.

```
In worktree /Users/sam/code/journal-app-hardening (branch fix/priority-hardening), continue docs/superpowers/plans/2026-06-09-priority-hardening.md from Task 3 onward, subagent-driven. Skip the Task 2 blocker for now: stash or commit the uncommitted lib/inventoryEarning.ts JSDoc/guard changes, but move the failing concurrency test to it.skip with a TODO referencing docs/nextPrompt20260609-150941.md. Track "make earning idempotent (lastEarnedDay in metadata)" as a follow-up task before final review (Task 12). Never touch dev.db or prod.
```

---
Saved to: `docs/nextPrompt20260609-150941.md`
